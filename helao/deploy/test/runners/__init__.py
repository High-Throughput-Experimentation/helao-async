"""Standalone MicroOrch example runners for the ``test`` deployment.

Each module here reproduces the action/experiment/sequence series of one of
the orchestrator-driven libraries under ``helao/deploy/test/`` using
:class:`helao.framework.runners.micro_orch.MicroOrch` instead of the full
orchestrator service. See ``README.md`` in this directory for how to launch
the required action servers and run the scripts.
"""
